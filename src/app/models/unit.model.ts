export class UnitListItem {
    public name: string;
    public symbol: string;
    public image: string;

    constructor(name: string, symbol: string, image: string) {
        this.name = name;
        this.symbol = symbol;
        this.image = image;
    }
}